export interface PageProps {
  searchParams: {
    [key: string]: string | number;
  };
  params: {
    [key: string]: string | number;
  };
}
export interface SearchParamsType {
  [key: string]: string | number;
}
